apiVersion: 1

providers:
  - name: "reclamos-dashboards"
    orgId: 1
    folder: "Sistema de Reclamos"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
